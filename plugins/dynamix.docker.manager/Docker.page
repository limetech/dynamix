Menu="Tasks:60"
Type="xmenu"
---
<?PHP
// array must be started since that's where we store the docker volume
//
if ($var['fsState'] != "Started") {
  echo "<p class='notice'>Array must be Started to manage Docker.</p>";
  return;
}
?>